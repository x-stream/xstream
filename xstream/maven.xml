<project>

    <goal name="deploy"
        description="Deploy website, jar and distribution releases"
        prereqs="clean, site:deploy, dist:deploy">
        <echo>Now run maven deploy-jar</echo>
        <!-- annoyingly, I have to run dist:deploy and jar:deploy as separate
             maven invocations - I think -->
    </goal>

    <goal name="deploy-jar" prereqs="jar:deploy"/>

</project>
